{% extends 'layout_ag.html.twig' %}
{% block title %}<title>Connexion {{monApplication.Appli}}</title>{% endblock %}
{% block body %}<body  class="page not-home">{% endblock %}


{% block main %}
<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            Vous êtes connectés avec l'adresse :  {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Déconnexion</a>
        </div>
    {% endif %}
    {% for message in app.flashes('warning') %}
    <div class="alert alert-warning">
        {{ message }}
    </div>
{% endfor %}

    <h5 class="h3 mb-3 font-weight-normal"> Connectez-vous  </h5>
    <label for="inputEmail">Email</label>
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
    <label for="inputPassword">Password</label>
    <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

    <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"
    >
    

     <form method="post" action="{{ path('app_login') }}">
     
    <button style="margin-top: 30px;" , type="submit"> Se connecter !  </button>
    {% if not app.user %}
    <h6 style="margin-top: 20px;"><a href="{{ path('app_register') }}"> Je n'ai pas de compte local, m'inscrire ! </a></h6>
    {% endif %}
</form>
{% endblock %}