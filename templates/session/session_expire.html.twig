{% extends 'layout_ag.html.twig' %}

{% block title %}
    <title>Session Expirée</title>
{% endblock %}

{% block main %}
    <div class="container mt-5 text-center">
        <h1>Session Expirée</h1>
        <p>{{ message }}</p>
        <a href="{{ path('home') }}" class="btn btn-primary mt-3">Retour à l'accueil</a>
    </div>
{% endblock %}
