{% extends 'layout_ag.html.twig' %}
{% block title %}<title> Inscription - {{monApplication.Appli}}</title>{% endblock %}
{% block body %}<body  class="page not-home">{% endblock %}

{% block main %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">M'inscrire</button>
    {{ form_end(form) }}

    <h4 style="margin-top: 90px;"><a href="{{ path('connexion') }}">J'ai deja un compte local'</a></h4>
{% endblock %}
