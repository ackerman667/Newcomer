{# templates/activation/success_activation.html.twig #}
{% extends 'layout_ag.html.twig' %}

{% block title %}<title>Activation réussie - {{ monApplication.Appli }}</title>{% endblock %}

{% block main %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <h2>Activation réussie</h2>
            <p>Votre compte a été activé avec succès.</p>
            <a href="{{ path('demande_externe', {'token': token}) }}" class="btn btn-primary">Accéder aux demandes.</a>
        </div>
    </div>
</div>
{% endblock %}
